# Core
sympy==1.13.3

# llama-cpp-python is optional — loaded at runtime if available (Docker/local only)
# Omitted here because Streamlit Cloud cannot compile it from source
# The pipeline falls through to Fireworks when local model is unavailable

# Env loading for local dev
python-dotenv==1.0.1
